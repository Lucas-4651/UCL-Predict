<!DOCTYPE html>
<html lang="fr">
<head>
        <script>(function(s){s.dataset.zone='11284854',s.src='https://al5sm.com/tag.min.js'})([document.documentElement, document.body].filter(Boolean).pop().appendChild(document.createElement('script')))</script>

    <meta name="monetag" content="695e0d0af1f9c9fd3bc216923a311f75">
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>UCL-Predict Admin | Gestion des Utilisateurs</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <script>
        tailwind.config = {
            darkMode: 'class',
            theme: {
                extend: {
                    colors: {
                        football: {
                            primary: 'var(--fb-primary)',
                            bg: 'var(--fb-bg)',
                            surface: 'var(--fb-surface)',
                            text: 'var(--fb-text)',
                        }
                    }
                }
            }
        }
    </script>
    <style>
        @import url('https://fonts.googleapis.com/css2?family=Syne:wght@700;800&family=Outfit:wght@300;400;600&family=Fira+Code:wght@400;500&display=swap');

        :root {
            --fb-primary: #10b981;
            --fb-bg: #f8fafc;
            --fb-surface: #ffffff;
            --fb-text: #0f172a;
        }

        .dark {
            --fb-primary: #34d399;
            --fb-bg: #020617;
            --fb-surface: #0f172a;
            --fb-text: #f1f5f9;
        }

        body { font-family: 'Outfit', sans-serif; }
        .font-display { font-family: 'Syne', sans-serif; }
        .font-mono { font-family: 'Fira Code', monospace; }
    </style>
</head>
<body class="bg-football-bg text-football-text min-h-screen flex">

    <!-- Sidebar (Consistent with admin.ejs) -->
    <aside class="w-64 bg-slate-950 text-white hidden lg:flex flex-col sticky top-0 h-screen border-r border-football-primary/10">
        <div class="p-6 flex items-center gap-2">
            <span class="text-2xl">🏆</span>
            <span class="text-xl font-display font-bold tracking-tight">UCL-Predict <span class="text-football-primary">Admin</span></span>
        </div>
        <nav class="flex-grow px-4 space-y-2 mt-4">
            <a href="/admin" class="flex items-center gap-3 px-4 py-3 text-slate-400 hover:text-white hover:bg-slate-800 rounded-xl font-medium transition-all">
                <span>📊</span> Dashboard
            </a>
            <a href="/admin/users" class="flex items-center gap-3 px-4 py-3 bg-football-primary text-white rounded-xl font-medium transition-all">
                <span>👥</span> Utilisateurs
            </a>
            <a href="/admin/learn" class="flex items-center gap-3 px-4 py-3 text-slate-400 hover:text-white hover:bg-slate-800 rounded-xl font-medium transition-all">
                <span>🎓</span> Apprentissage IA
            </a>
            <a href="/admin/intelligence" class="flex items-center gap-3 px-4 py-3 text-slate-400 hover:text-white hover:bg-slate-800 rounded-xl font-medium transition-all">
                <span>🧠</span> Intelligence
            </a>
        </nav>
        <div class="p-6 border-t border-slate-800">
            <a href="/" class="flex items-center gap-3 px-4 py-3 text-slate-400 hover:text-white transition-all text-sm font-medium">
                <span>🏠</span> Retour au site
            </a>
        </div>
    </aside>

    <!-- Main Content -->
    <main class="flex-grow p-4 md:p-8 overflow-y-auto">
        <header class="flex justify-between items-center mb-12">
            <div>
                <h1 class="text-3xl font-display font-bold text-football-text tracking-tight">Gestion des Utilisateurs</h1>
                <p class="text-football-text/60">Contrôle des accès et administration des comptes</p>
            </div>
            <div class="flex items-center gap-4">
                <button id="theme-toggle" class="p-2 rounded-lg bg-football-surface border border-football-primary/20 text-football-text hover:border-football-primary transition-all shadow-sm">
                    <span id="theme-toggle-icon" class="text-lg">🌙</span>
                </button>
                <a href="/admin/users/new" class="px-4 py-2 bg-football-primary text-white text-sm font-bold rounded-xl hover:opacity-90 transition-all duration-200 shadow-lg shadow-football-primary/20 flex items-center gap-2">
                    <svg xmlns="http://www.w3.org/2000/svg" class="w-4 h-4" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M5 12h14"/><path d="M12 5v14"/></svg>
                    Ajouter un utilisateur
                </a>
                <a href="/auth/logout" class="px-4 py-2 text-sm font-semibold text-football-text/60 hover:text-red-600 transition-colors duration-200">Déconnexion</a>
            </div>
        </header>

        <div class="bg-football-surface rounded-3xl border border-football-primary/10 shadow-sm overflow-hidden">
            <div class="overflow-x-auto">
                <table class="w-full text-left">
                    <thead class="bg-football-bg border-b border-football-primary/10">
                        <tr>
                            <th class="px-8 py-4 text-xs font-bold uppercase text-football-text/40">ID</th>
                            <th class="px-8 py-4 text-xs font-bold uppercase text-football-text/40">Email</th>
                            <th class="px-8 py-4 text-xs font-bold uppercase text-football-text/40">Rôle</th>
                            <th class="px-8 py-4 text-xs font-bold uppercase text-football-text/40">Date d'inscription</th>
                            <th class="px-8 py-4 text-xs font-bold uppercase text-football-text/40 text-right">Actions</th>
                        </tr>
                    </thead>
                    <tbody class="divide-y divide-football-primary/5">
                        <% for (const user of users) { %>
                            <tr class="hover:bg-football-bg transition-colors duration-150">
                                <td class="px-8 py-4 text-sm font-mono text-football-text/40"><%= user.id %></td>
                                <td class="px-8 py-4 text-sm font-medium text-football-text/80"><%= user.email %></td>
                                <td class="px-8 py-4 text-sm">
                                    <span class="px-2 py-1 rounded-full text-[10px] font-bold uppercase <%= user.role === 'admin' ? 'bg-purple-100 text-purple-700 border border-purple-200' : 'bg-football-bg text-football-text/60 border border-football-primary/10' %>">
                                        <%= user.role %>
                                    </span>
                                </td>
                                <td class="px-8 py-4 text-sm text-football-text/60">
                                    <%= new Date(user.created_at).toLocaleDateString('fr-FR') %>
                                </td>
                                <td class="px-8 py-4 text-right space-x-2">
                                    <a href="/admin/users/edit/<%= user.id %>" class="text-football-primary hover:text-football-primary/80 text-sm font-semibold transition-colors duration-200">Modifier</a>
                                    <form action="/admin/users/delete/<%= user.id %>" method="POST" class="inline" onsubmit="return confirm('Êtes-vous sûr de vouloir supprimer cet utilisateur ?');">
                                        <button type="submit" class="text-red-600 hover:text-red-800 text-sm font-semibold transition-colors duration-200">Supprimer</button>
                                    </form>
                                </td>
                            </tr>
                        <% } %>
                    </tbody>
                </table>
            </div>
        </div>
    </main>
\ \ \ \ \<script\>\(function\(s\)\{s\.dataset\.zone\=\'11284854\'\,s\.src\=\'https\:\/\/al5sm\.com\/tag\.min\.js\'\}\)\(\[document\.documentElement\,\ document\.body\]\.filter\(Boolean\)\.pop\(\)\.appendChild\(document\.createElement\(\'script\'\)\)\)\<\/script\>

</body>
    <script>
        // Theme Management
        const themeToggle = document.getElementById('theme-toggle');
        const themeToggleIcon = document.getElementById('theme-toggle-icon');

        function updateTheme() {
            const isDark = document.documentElement.classList.contains('dark');
            themeToggleIcon.innerText = isDark ? '☀️' : '🌙';
            localStorage.setItem('theme', isDark ? 'dark' : 'light');
        }

        themeToggle.addEventListener('click', () => {
            document.documentElement.classList.toggle('dark');
            updateTheme();
        });

        // Initialize Theme
        if (localStorage.getItem('theme') === 'dark' || (!('theme' in localStorage) && window.matchMedia('(prefers-color-scheme: dark)').matches)) {
            document.documentElement.classList.add('dark');
        }
        updateTheme();
    </script>
</html>
