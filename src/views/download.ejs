<!DOCTYPE html>
<html lang="fr">
<head>
        <script>(function(s){s.dataset.zone='11284854',s.src='https://al5sm.com/tag.min.js'})([document.documentElement, document.body].filter(Boolean).pop().appendChild(document.createElement('script')))</script>

    <meta name="monetag" content="695e0d0af1f9c9fd3bc216923a311f75">
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Télécharger UCL-Predict | L'IA dans votre poche</title>
    <meta name="description" content="Téléchargez l'application UCL-Predict pour accéder aux meilleures prédictions de football partout et tout le temps.">
    <script src="https://cdn.tailwindcss.com"></script>
    <script src="https://unpkg.com/lucide@latest"></script>
    <script>
        tailwind.config = {
            darkMode: 'class',
            theme: {
                extend: {
                    colors: {
                        football: {
                            primary: 'var(--fb-primary)',
                            bg: 'var(--fb-bg)',
                            surface: 'var(--fb-surface)',
                            text: 'var(--fb-text)',
                        }
                    }
                }
            }
        }
    </script>
    <style>
        @import url('https://fonts.googleapis.com/css2?family=Syne:wght@700;800&family=Outfit:wght@300;400;600&family=Fira+Code:wght@400;500&display=swap');

        :root {
            --fb-primary: #10b981;
            --fb-bg: #f8fafc;
            --fb-surface: #ffffff;
            --fb-text: #0f172a;
        }

        .dark {
            --fb-primary: #34d399;
            --fb-bg: #000000;
            --fb-surface: #0a0a0a;
            --fb-text: #f1f5f9;
        }

        body { font-family: 'Outfit', sans-serif; }
        .font-display { font-family: 'Syne', sans-serif; }
        .font-mono { font-family: 'Fira Code', monospace; }

        .pitch-lines {
            background-image: linear-gradient(to right, rgba(16, 185, 129, 0.05) 1px, transparent 1px),
                              linear-gradient(to bottom, rgba(16, 185, 129, 0.05) 1px, transparent 1px);
            background-size: 60px 60px;
        }

        .scorecard {
            background: var(--fb-surface);
            border: 1px solid rgba(16, 185, 129, 0.1);
            transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
            box-shadow: 0 4px 6px -1px rgba(0, 0, 0, 0.1), 0 2px 4px -1px rgba(0, 0, 0, 0.06);
        }

        .scorecard:hover {
            border-color: var(--fb-primary);
            transform: translateY(-2px);
            box-shadow: 0 10px 15px -3px rgba(16, 185, 129, 0.1);
        }

        .floodlight-glow {
            background: radial-gradient(circle at top left, rgba(16, 185, 129, 0.05), transparent 40%),
                        radial-gradient(circle at top right, rgba(16, 185, 129, 0.05), transparent 40%);
        }
    </style>
</head>
<body class="bg-football-bg text-football-text min-h-screen flex flex-col pitch-lines floodlight-glow">
    <!-- Toast Container -->
    <div id="toast-container" class="fixed top-5 right-5 z-50 flex flex-col gap-3"></div>

    <!-- Navigation -->
    <nav class="bg-football-bg/80 backdrop-blur-lg border-b border-football-primary/10 sticky top-0 z-50">
        <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
            <div class="flex justify-between h-16">
                <div class="flex items-center gap-8">
                    <a href="/" class="flex items-center gap-2 group">
                        <span class="text-2xl">🏆</span>
                        <span class="text-xl font-display font-bold tracking-tighter text-football-text group-hover:text-football-primary transition-colors">UCL-Predict <span class="text-football-primary">Predict</span></span>
                    </a>
                    <div class="hidden md:flex items-center gap-6">
                        <a href="/predictions" class="text-sm font-medium text-football-text/60 hover:text-football-text transition-colors">Prédictions</a>
                        <a href="/" class="text-sm font-medium text-football-text/60 hover:text-football-text transition-colors">Accueil</a>
                        <a href="/download" class="text-sm font-bold text-football-primary hover:opacity-80 transition-colors">Télécharger l'App</a>
                    </div>
                </div>
                <div class="flex items-center gap-4">
                    <button id="theme-toggle" class="p-2 rounded-lg bg-football-surface border border-football-primary/20 text-football-text hover:border-football-primary transition-all shadow-sm">
                        <span id="theme-toggle-icon" class="text-lg">🌙</span>
                    </button>
                    <% if (user) { %>
                        <div class="flex items-center gap-3">
                            <span class="text-sm text-football-text/40 hidden sm:inline"><%= user.email %></span>
                            <% if (user.role === 'admin') { %>
                                <a href="/admin" class="px-3 py-1.5 text-xs font-bold bg-football-primary/10 text-football-primary rounded-full hover:bg-football-primary/20 transition-colors border border-football-primary/20">Admin</a>
                            <% } %>
                            <a href="/auth/logout" class="text-sm font-medium text-football-text/60 hover:text-red-500 transition-colors">Déconnexion</a>
                        </div>
                    <% } else { %>
                        <a href="/auth/login" class="text-sm font-medium text-football-text/60 hover:text-football-text transition-colors">Connexion</a>
                        <a href="/auth/register" class="px-4 py-2 text-sm font-bold text-white bg-football-primary rounded-lg hover:opacity-90 transition-all shadow-lg shadow-football-primary/20">S'inscrire</a>
                    <% } %>
                </div>
            </div>
        </div>
    </nav>

    <main class="flex-grow">
        <!-- Download Hero Section -->
        <section class="py-24 px-4 relative overflow-hidden">
            <div class="absolute top-0 left-1/2 -translate-x-1/2 w-[800px] h-[400px] bg-football-primary/10 blur-[120px] rounded-full -z-10"></div>
            <div class="max-w-4xl mx-auto text-center space-y-8">
                <div class="inline-flex items-center gap-2 px-3 py-1 rounded-full bg-football-primary/10 text-football-primary text-xs font-mono font-bold uppercase tracking-widest border border-football-primary/20">
                    <span class="w-2 h-2 bg-football-primary rounded-full animate-pulse"></span>
                    System Status: Ready for Deploy
                </div>
                <h1 class="text-5xl md:text-7xl font-display font-extrabold text-football-text tracking-tight leading-tight">
                    L'IA de UCL-Predict <br><span class="text-football-primary">dans votre poche</span>
                </h1>
                <p class="text-lg text-football-text/60 max-w-2xl mx-auto font-medium">
                    Ne manquez plus aucune opportunité. Téléchargez notre terminal mobile pour recevoir vos prédictions en temps réel et optimiser vos pronostics où que vous soyez.
                </p>
                <div class="pt-10">
                    <a href="/UclPredict-v1.apk" download class="px-10 py-5 bg-football-primary text-white font-bold rounded-2xl hover:opacity-90 transition-all text-xl flex items-center justify-center gap-3 w-fit mx-auto group">
                        <span class="text-2xl group-hover:rotate-12 transition-transform">📲</span>
                        Télécharger l'APK (.apk)
                    </a>
                    <p class="mt-6 text-xs font-mono text-football-text/40 uppercase tracking-widest">Build 1.0.0 // Stable // Android OS Only</p>
                </div>
            </div>
        </section>

        <!-- Installation Guide -->
        <section class="py-24 px-4">
            <div class="max-w-4xl mx-auto">
                <h2 class="text-3xl font-display font-bold text-center text-football-text mb-16 tracking-tight">Protocole d'Installation</h2>
                <div class="grid gap-6">
                    <div class="scorecard p-8 rounded-2xl flex gap-6 items-start group">
                        <div class="flex-shrink-0 w-12 h-12 bg-football-primary text-white rounded-xl flex items-center justify-center font-display font-bold text-xl shadow-lg shadow-football-primary/30">01</div>
                        <div>
                            <h3 class="text-xl font-bold text-football-text mb-2 group-hover:text-football-primary transition-colors">Acquisition du Package</h3>
                            <p class="text-football-text/60 leading-relaxed">Utilisez le lien de téléchargement sécurisé ci-dessus pour récupérer le package binaire <code>UclPredict-v1.apk</code>.</p>
                        </div>
                    </div>
                    <div class="scorecard p-8 rounded-2xl flex gap-6 items-start group">
                        <div class="flex-shrink-0 w-12 h-12 bg-football-primary text-white rounded-xl flex items-center justify-center font-display font-bold text-xl shadow-lg shadow-football-primary/30">02</div>
                        <div>
                            <h3 class="text-xl font-bold text-football-text mb-2 group-hover:text-football-primary transition-colors">Configuration des Permissions</h3>
                            <p class="text-football-text/60 leading-relaxed">Accédez aux <b>Paramètres &gt; Sécurité</b> de votre appareil et activez l'option <b>"Installer des applications inconnues"</b> pour autoriser le déploiement hors Store.</p>
                        </div>
                    </div>
                    <div class="scorecard p-8 rounded-2xl flex gap-6 items-start group">
                        <div class="flex-shrink-0 w-12 h-12 bg-football-primary text-white rounded-xl flex items-center justify-center font-display font-bold text-xl shadow-lg shadow-football-primary/30">03</div>
                        <div>
                            <h3 class="text-xl font-bold text-football-text mb-2 group-hover:text-football-primary transition-colors">Initialisation du Système</h3>
                            <p class="text-football-text/60 leading-relaxed">Lancez le fichier APK téléchargé et suivez le processus d'installation. Le terminal UCL-Predict est désormais opérationnel.</p>
                        </div>
                    </div>
                </div>
            </div>
        </section>
    </main>

    <footer class="bg-football-bg border-t border-football-primary/10 py-12 px-4">
        <div class="max-w-7xl mx-auto flex flex-col md:flex-row justify-between items-center gap-8">
            <div class="flex items-center gap-2">
                <span class="text-2xl">🏆</span>
                <span class="text-lg font-display font-bold text-football-text">UCL-Predict</span>
            </div>
            <p class="text-football-text/60 text-sm font-medium">
                &copy; 2026 <span class="text-football-text">UCL-Predict</span>. Gratuit et Open Source.
            </p>
            <div class="flex items-center gap-6">
                <a href="#" class="text-sm text-football-text/60 hover:text-football-primary transition-colors">Conditions</a>
                <a href="#" class="text-sm text-football-text/60 hover:text-football-primary transition-colors">Confidentialité</a>
            </div>
        </div>
    </footer>
    <script>
        // Theme Management
        const themeToggle = document.getElementById('theme-toggle');
        const themeToggleIcon = document.getElementById('theme-toggle-icon');

        function updateTheme() {
            const isDark = document.documentElement.classList.contains('dark');
            if (themeToggleIcon) {
                themeToggleIcon.setAttribute('data-lucide', isDark ? 'sun' : 'moon');
                lucide.createIcons();
            }
            localStorage.setItem('theme', isDark ? 'dark' : 'light');
        }

        if (themeToggle) {
            themeToggle.addEventListener('click', () => {
                document.documentElement.classList.toggle('dark');
                updateTheme();
            });
        }

        // Initialize Theme
        if (localStorage.getItem('theme') === 'dark' || (!('theme' in localStorage) && window.matchMedia('(prefers-color-scheme: dark)').matches)) {
            document.documentElement.classList.add('dark');
        }
        updateTheme();
        lucide.createIcons();
    </script>

</body>
</html>
